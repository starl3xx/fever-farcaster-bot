NEYNAR_API_KEY=
NEYNAR_SIGNER_UUID=
FARCASTER_CHANNEL_ID=fever
FC_CUSTODY_MNEMONIC=
# Optional backstop. The bot's own Farcaster fid, used as the reply parent when
# a late-arriving recap mp4 upgrades an image-fallback cast to native video. The
# fid is normally captured from the fallback post response; set this only if that
# ever returns no author fid. This bot posts as starl3xx (fid 6500).
FARCASTER_FID=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
CRON_SECRET=
YOUTUBE_API_KEY=
BOT_ENABLED=false
